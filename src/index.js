import React from "react";
import ReactDOM from "react-dom";
import Main from './components/Main'; // navnet skal være med stort?

ReactDOM.render(<Main />, document.getElementById("root")); //this is a DOM which react uses and convert to
